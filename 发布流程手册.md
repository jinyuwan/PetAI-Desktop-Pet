# PetAI 更新与发布流程手册

本文件记录 PetAI 桌宠从**开发 → 提交 → 发版 → 上传 GitHub → 自动打包**的完整流程，供日常迭代维护使用。

---

## 目录结构速览

```
project_pet/
├─ pet-ai/                    ← ★ 唯一开发目录（就是 GitHub 仓库根目录）
│  ├─ .git/                   ← git 仓库（只在这里）
│  ├─ .github/workflows/release.yml  ← GitHub Actions 自动打包配置
│  ├─ main.js / preload.js    ← 主进程代码
│  ├─ renderer/               ← 界面代码（app/chat/settings）
│  ├─ skins/                  ← 皮肤目录（素材被 gitignore，不进仓库）
│  ├─ assets/                 ← 图标资源
│  ├─ README.md / CHANGELOG.md / LICENSE
│  └─ dist/                   ← 本地打包产物（gitignore，不进仓库）
├─ _archive/                  ← 封存旧代码/素材（不参与开发）
├─ Q版蕾米/                    ← 皮肤原始素材备份（不入仓库）
└─ desktop-pet-skin-design/   ← 皮肤设计画布工程（TRAE 使用中，勿动）
```

> ⚠️ **重要**：`pet-ai` 是唯一开发目录。所有改动只在这里做，不要在其他目录修改代码。

---

## 一、本地开发流程

### 1. 启动开发环境

```bash
cd g:\files\ai\project_pet\pet-ai
npm start        # 启动桌宠（开发模式）
```

### 2. 修改代码

- **主进程逻辑**：`main.js`（AI 对话、会话管理、截屏、窗口、托盘）
- **界面逻辑**：`renderer/` 下 `app.js`（宠物状态机）、`chat.js`（对话框）、`settings.js`（设置面板）
- **样式**：`renderer/*.css`
- **新增功能需同步**：
  - `main.js` 加 IPC → `preload.js` 加桥接 → `renderer/` 里调用
  - 修改 `CHANGELOG.md` 记录变更

### 3. 语法检查（改完必做）

```bash
node --check main.js
node --check preload.js
node --check renderer/app.js
node --check renderer/chat.js
node --check renderer/settings.js
```

全部无输出 = 通过。

---

## 二、提交代码到 GitHub

### 日常小更新（不需要发新版时）

```bash
cd g:\files\ai\project_pet\pet-ai
git add .
git commit -m "feat: 新增 xxx 功能"        # 或 fix: / docs: / refactor: / ci:
git push
```

提交信息规范（约定式提交）：

| 前缀 | 含义 | 示例 |
|---|---|---|
| `feat:` | 新功能 | `feat: 新增联网搜索开关` |
| `fix:` | 修 bug | `fix: 清空历史后对话框未重置` |
| `docs:` | 文档 | `docs: 更新 README` |
| `refactor:` | 重构 | `refactor: 拆分动作调度模块` |
| `ci:` | 构建/CI | `ci: 修复 Actions 权限` |

> 只有打 tag 才会触发自动打包，普通 `git push` 只是更新代码，不会出安装包。

---

## 三、发布新版（自动打包）

### 前置检查

1. 确认代码已提交并推送（`git status` 工作区干净）
2. 确认 `package.json` 里的 `version` 已更新到新版本号
3. 确认 `CHANGELOG.md` 已记录本次变更

### 发布命令（核心）

```bash
cd g:\files\ai\project_pet\pet-ai
git tag v1.1.0                # ① 打标签（版本号与 package.json 一致）
git push origin v1.1.0        # ② 推送标签 → 自动触发 GitHub Actions
```

### 之后等待约 90 秒

GitHub Actions 云端自动完成：

1. 检出代码
2. 安装依赖（`npm ci`）
3. 运行 `electron-builder` 打包 Windows 安装包
4. 上传到 Release 页面

### 验证发布成功

打开仓库 Release 页确认：

```
https://github.com/jinyuwan/PetAI-Desktop-Pet/releases
```

应看到新版本 + 安装包附件（`PetAI.Setup.1.1.0.exe`）。

也可以查 API 看构建状态：

```
https://api.github.com/repos/jinyuwan/PetAI-Desktop-Pet/actions/runs?per_page=3
```

`conclusion: success` = 成功，`failure` = 失败（点开对应 run 看哪一步红）。

---

## 四、本地手动打包（备用，不进 GitHub）

> 通常不需要：Actions 会自动出包。仅当需要**本地验证**或**离线分享**时用。

```bash
cd g:\files\ai\project_pet\pet-ai
npm run dist
```

产物在 `pet-ai/dist/`：

| 文件 | 用途 |
|---|---|
| `dist/PetAI Setup <版本>.exe` | 安装包（分享给别人） |
| `dist/win-unpacked/PetAI.exe` | 免安装版（自己用，双击运行） |

---

## 五、常见问题排查

### Q1: 推送被拒绝（`! [rejected]`）
远程有本地没有的提交。先拉取合并：
```bash
git pull origin main --rebase
git push
```
> 若确定本地才是最新（远程是残缺旧版），可强制覆盖：
> ```bash
> git push --force origin main
> ```

### Q2: Actions 构建失败
1. 打开仓库 → Actions → 点击失败的 run → 看红 X 步骤
2. 常见原因：
   - **上传 Release 失败**：workflow 缺 `permissions: contents: write`（已修复，勿删）
   - **npm ci 失败**：检查 `package-lock.json` 是否随代码提交
   - **打包失败**：看日志具体报错（如图标尺寸、依赖缺失）
3. 修复后：重新 commit + push + 删旧标签重打：
```bash
git tag -d v1.1.0
git push origin :refs/tags/v1.1.0
git tag v1.1.0
git push origin v1.1.0
```

### Q3: 想改自动打包行为
编辑 `.github/workflows/release.yml`：
- 加 `mac` / `linux` 打包：在 `build.win` 同级加 `build.mac` / `build.linux`
- 改触发条件：修改 `on.push.tags`
- 改产物：修改 `files` 与 `softprops/action-gh-release` 的 `files`

### Q4: 数据在哪（不影响代码）
- 对话历史：`%APPDATA%/desktop-pet/chat-history.json`
- AI 配置：`%APPDATA%/desktop-pet/ai-config.json`
- 这些在系统用户目录，**不进 git、不进安装包**，删代码/重装不影响

---

## 六、隐私与安全注意

- API Key 只存在本机 `%APPDATA%`，不随代码/安装包分发
- 皮肤素材（版权原因）不入公开仓库，只在本地 `skins/` 保留
- 发给别人用：直接发 Release 链接（如 `https://github.com/jinyuwan/PetAI-Desktop-Pet/releases/download/v1.1.0/PetAI.Setup.1.1.0.exe`）

---

## 常用命令速查

```bash
# 开发
cd g:\files\ai\project_pet\pet-ai && npm start

# 提交
git add . && git commit -m "feat: xxx" && git push

# 发版
git tag v1.1.0 && git push origin v1.1.0

# 本地打包
npm run dist

# 查看状态
git status / git log --oneline
```
